# BEGIN dotfiles managed codex rules
prefix_rule(
    pattern = ["/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", "--headless=new"],
    decision = "allow",
    justification = "Allow Codex to run Chrome headless checks without repeated approval.",
)
# END dotfiles managed codex rules

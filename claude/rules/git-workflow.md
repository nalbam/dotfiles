# Git Workflow

- **명시적 지시 없이 commit·push하지 않는다.** 커밋 허가를 push 허가로 확대하지 않는다.
- 기존 작업 트리·index의 무관한 변경을 되돌리거나 본인 커밋에 섞지 않는다.
- 변경 폐기·force push·공개 이력 재작성·브랜치 삭제·훅/서명 우회는 사용자가 분명히 요청한 경우에만 수행한다.
- 커밋 메시지·PR 본문에 `Co-Authored-By`, `Generated with`, 도구 귀속 푸터·배지를 넣지 않는다.
